// This file is now obsolete - we use Convex directly instead of API clients
// Keeping it for now in case any imports reference it, but it should be removed in Phase 3
// when we implement the real FastAPI integration

export const apiClient = {
  // Placeholder - all functionality moved to Convex queries/mutations
};
